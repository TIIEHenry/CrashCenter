<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/shell_press_surface"
    android:minHeight="@dimen/list_item_min_height"
    android:paddingHorizontal="@dimen/content_padding_horizontal"
    android:paddingVertical="@dimen/spacing_md">

    <TextView
        android:id="@+id/tvExceptionClass"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="15sp"
        android:textStyle="bold"
        android:layout_marginEnd="@dimen/spacing_sm"
        app:layout_constraintBottom_toTopOf="@id/tvMessage"
        app:layout_constraintEnd_toStartOf="@id/badgeRow"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        tools:text="NullPointerException" />

    <LinearLayout
        android:id="@+id/badgeRow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/tvOutcomeBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="@dimen/badge_padding_horizontal"
            android:paddingVertical="@dimen/badge_padding_vertical"
            android:textSize="11sp"
            android:visibility="gone"
            tools:text="仅监测"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/tvSourceBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_xs"
            android:background="@drawable/bg_system_badge"
            android:paddingHorizontal="@dimen/badge_padding_horizontal"
            android:paddingVertical="@dimen/badge_padding_vertical"
            android:textColor="@color/systemAppBadge"
            android:textSize="11sp"
            android:visibility="gone"
            tools:text="UEH"
            tools:visibility="visible" />
    </LinearLayout>

    <TextView
        android:id="@+id/tvMessage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="?android:attr/textColorSecondary"
        android:textSize="12sp"
        app:layout_constraintBottom_toTopOf="@id/tvTimestamp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvExceptionClass"
        tools:text="Attempt to invoke virtual method…" />

    <TextView
        android:id="@+id/tvTimestamp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textColor="@color/textTertiary"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvMessage"
        tools:text="3 min ago" />

</androidx.constraintlayout.widget.ConstraintLayout>
